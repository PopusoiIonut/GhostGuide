name: "Ghost Guide AR"
targets:
  "Ghost Guide AR":
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources: [Sources]
    info:
      path: Sources/Info.plist
      properties:
        UIBackgroundModes:
          - audio
          - location
        UIRequiresFullScreen: YES
        NSLocationAlwaysAndWhenInUseUsageDescription: "GhostGuide uses your location in the background to detect invisible tripwires around historical monuments."
        NSLocationWhenInUseUsageDescription: "GhostGuide needs to see where you are to find nearby stories."
        UILaunchScreen: {}
    settings:
      base:
        DEVELOPMENT_TEAM: M9NL4PJ9AL
        PRODUCT_BUNDLE_IDENTIFIER: com.popusoiionut.GhostGuideAR
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ACCENT_COLOR_NAME: AccentColor
schemes:
  "Ghost Guide AR":
    build:
      targets:
        "Ghost Guide AR": all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
